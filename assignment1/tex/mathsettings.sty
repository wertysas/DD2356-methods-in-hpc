%%
%% Provides definitions of Theorem environments and special math commands.
%%

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{mathsettings}[2020/11/30 Math Settings package]


%% Theorem Definitions
\RequirePackage{amsmath}
\RequirePackage{amsthm}
\RequirePackage[most]{tcolorbox}
\RequirePackage{xparse}

% Theorem definitions
\newtheorem{lem}{Lemma}[section]
\newtheorem{thm}{Theorem}[section]
\newtheorem*{corollary}{Corollary}

\theoremstyle{definition}
\newtheorem{definition}{Definition}[section]
\newtheorem*{definition*}{Definition}
\newtheorem{example}{Example}[section]
\newtheorem{exercise}{Exercise}[section]
\newtheorem{problem}{Problem}[section]
\renewcommand{\qedsymbol}{$\blacksquare$}
\renewenvironment{proof}{\bfseries Proof.}
{\begin{flushright} \qedsymbol \end{flushright}}

\makeatletter
\def\th@newremark{\th@remark\thm@headfont{}}
\makeatletter
\theoremstyle{newremark}
\newtheorem*{remark}{\textsc{Remark}}
\newtheorem*{btmline}{\textsc{Bottom line}}

% Theorem styles
\newcounter{testexample}
\tcbset{before upper={\parindent0em}}
%%% See page 373 in the tcolorbox manual
%\tcolorboxenvironment{thm}{oversize, breakable, %colback = green!10!white,
%before skip=10pt, after skip=10pt,
%colframe = white, sharp corners}

%\tcolorboxenvironment{thm}{breakable, before skip=10pt,after skip=10pt, colback = yellow!20!white, colframe = white, sharp corners}
%\tcolorboxenvironment{lem}{oversize, breakable, before skip=10pt,after skip=10pt, colframe = white, sharp corners}

%\tcolorboxenvironment{definition}{breakable, colback = white!98!black,
%colframe = white!50!black, before skip=10pt,after skip=10pt, sharp corners}
%\tcolorboxenvironment{remark}{breakable, colback = red!10!white, colframe = black, before = \noindent, sharp corners}
\tcolorboxenvironment{essence}{breakable, colback = green!15!white, colframe = white, before = \noindent, sharp corners}
%\tcolorboxenvironment{example}{oversize, breakable, colback = green!10!white, colframe = white, before = \noindent, sharp corners}
%\tcolorboxenvironment{exercise}{breakable, colback = white, colframe = black, sharp corners}


%\tcolorboxenvironment{lem}{%blanker,
%breakable,%colback=white,
%left=5mm,before skip=10pt,after skip=10pt,borderline west={0.5mm}{0pt}{red!75!black}}

%\tcolorboxenvironment{definition}{
%    blanker,
%    breakable,
%    left=3mm,
%    before skip=15pt,
%    after skip=15pt,
%    %borderline north={0.1mm}{-5pt},
%    %borderline south={0.1mm}{-5pt}
%}

%\tcolorboxenvironment{exercise}{
%    blanker,
%    breakable,
%    left=3mm,
%    before skip=15pt,
%    after skip=15pt,
%    borderline north={0.1mm}{-5pt},
%    borderline south={0.1mm}{-5pt}
%}
% To manage 

\endinput
