%%
%% Geometry and Page layout corresponding to Short Notes
%%

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{pagelayout}[2020/11/30 Page Layout package]


% Geometry Package lets u modify documents geometric features (marginsize etc.)
\RequirePackage[a4paper, total={6in, 10in}]{geometry}

% xcolor is used to color the title includes colors which can be specified by their dvipsnamens (see wiki table)
\RequirePackage[dvipsnames]{xcolor}

% Used to modify the title and author information
\RequirePackage{titling}

% parskip to skip a line between paragraphs
\RequirePackage[parfill]{parskip}

\RequirePackage{fancyhdr}




\pretitle{\begin{center}\LARGE\bfseries}%\sffamily}
\posttitle{\par\end{center}\vskip 0.5em}

% To manage 

\endinput